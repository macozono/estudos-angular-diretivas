import { DiretivaNgifDirective } from './diretiva-ngif.directive';

describe('DiretivaNgifDirective', () => {
  it('should create an instance', () => {
    const directive = new DiretivaNgifDirective();
    expect(directive).toBeTruthy();
  });
});
